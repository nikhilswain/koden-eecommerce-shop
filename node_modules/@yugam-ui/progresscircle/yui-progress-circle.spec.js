import { yuiProgressCircle } from './yui-progress-circle';
describe('yuiProgressCircle', () => {
  it('should work', () => {
    expect(yuiProgressCircle()).toEqual('yui-progress-circle');
  });
});
